node_modules
dist
.wrangler
.turbo
.env
.env.*
!.env.example
.dev.vars
.dev.vars.*
.DS_Store
*.log
coverage
CLAUDE.md
.vscode/*
!.vscode/extensions.json
copy-paste

# Local-only component/screen testing sandbox (see sandbox/README.md) -
# never committed, deliberately kept out of the tracked app/backend code.
/sandbox
